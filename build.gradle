// Gradle build script for the Minie-site-it project

plugins {
    id 'base' // to add a "clean" task to the project
}

// Register cleanup tasks:

clean.dependsOn('cleanNodeModules')
tasks.register('cleanNodeModules', Delete) {
    delete 'node_modules'
}